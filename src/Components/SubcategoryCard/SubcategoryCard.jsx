import React from "react";
import { Tab, TabList, TabPanel, Tabs } from "react-tabs";

const SubcategoryCard = ({ categories }) => {
  console.log(categories);
  //   const { image, image2 } = information;

  return (
    <div>
      <div></div>
    </div>
  );
};

export default SubcategoryCard;
